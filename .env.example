# =============================================================================
# BMAD Platform - Root Environment Configuration
# =============================================================================
# Copy this file to .env.local and update with your actual values
# Never commit actual secrets to version control

# Development Environment
NODE_ENV=development
LOG_LEVEL=debug

# Feature Flags
ENABLE_MONITORING=true
ENABLE_ANALYTICS=false
ENABLE_DEBUG_MODE=true
ENABLE_CHAOS_TESTING=false

# Monorepo Configuration  
WORKSPACE_ROOT=/path/to/bmadkit.com
BUILD_TARGET=development

# Global Security Settings
TRUST_PROXY=false
SECURE_COOKIES=false

# =============================================================================
# NOTE: Package-specific configurations are in individual package directories:
# - packages/api/.env.example (Backend API configuration)
# - packages/web/.env.example (Frontend configuration) 
# - packages/realtime/.env.example (WebSocket server configuration)
# =============================================================================